```
Usage: vcd vm download [OPTIONS] VAPP_NAME VM_NAME VC_HOST VC_USER VC_PASSWORD
                       GUEST_USER GUEST_PASSWORD SOURCE [OUTPUT]

Options:
  -h, --help  Show this message and exit.

```
